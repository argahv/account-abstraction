{
  "name": "account-abstraction-mvp",
  "version": "1.0.0",
  "description": "MVP for ERC-4337 style fund flow with RahatToken and CashToken",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy-contracts": "hardhat run scripts/deploy.js --network base_sepolia",
    "mint-and-transfer": "hardhat run scripts/mint_and_transfer.js --network base_sepolia",
    "assign": "hardhat run scripts/assign.js --network base_sepolia",
    "cashout": "hardhat run scripts/cashout.js --network base_sepolia"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.6",
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "@openzeppelin/contracts": "^4.9.0",
    "chai": "^4.3.4",
    "ethers": "^5.7.2",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "@account-abstraction/sdk": "^0.6.0",
    "@nomicfoundation/hardhat-toolbox": "^6.0.0",
    "dotenv": "^17.2.0"
  }
}
